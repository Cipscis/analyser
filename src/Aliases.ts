/**
 * A set of string[] alias lists, to be treated as through they were the same value for the purposes of filtering and grouping.
 * The first value in each list will be treated as the canonical value.
 */
type Aliases = string[][];

export { Aliases };
